{
  "name": "@madav/models",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Madav model provider layer — local model runtime management + provider integrations (distinct from @madav/core model selection).",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc -p tsconfig.build.json && node --test dist/test/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": { "typescript": "^5.5.0", "@types/node": "^20.14.0" }
}
