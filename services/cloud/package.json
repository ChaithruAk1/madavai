{
  "name": "@madav/cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Madav cloud — stateless modular-monolith gateway. Sessions/rate-limit/sync behind interfaces; Redis/Postgres slot in at deploy.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc -p tsconfig.build.json && node --test dist/test/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "@madav/contracts": "file:../../packages/contracts" },
  "devDependencies": { "typescript": "^5.5.0", "@types/node": "^20.14.0" }
}
