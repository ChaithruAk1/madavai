{
  "name": "brainedge-auth-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BrainEdge auth + trial + billing server (reference).",
  "main": "auth-server.mjs",
  "scripts": {
    "start": "node auth-server.mjs"
  },
  "engines": { "node": ">=18" },
  "//": "pg is only needed when DATABASE_URL is set (Postgres backend). JSON-file mode needs no deps.",
  "optionalDependencies": {
    "pg": "^8.13.1"
  }
}
